# Pinned exactly — DESIGN.md "Blur detection & determinism": a
# different opencv-python-headless build can decode JPEGs a few pixel
# values apart, which is enough to move the Laplacian variance and
# break the stress test's determinism assertion.
fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
psycopg2-binary==2.9.12
pydantic==2.13.5
pydantic-settings==2.15.0
opencv-python-headless==4.10.0.84
pika==1.3.2
alembic==1.14.0
python-dotenv==1.0.1
Pillow==11.3.0

# --- tests / stress test / smoke test ---
pytest==8.3.4
httpx==0.28.1
requests==2.32.3
